#include <stdio.h>

/**
 * add - add two numbers and return the result
 * @x: Number to add
 * @y: Number to add
 *
 * Return: result of addition of arguments
 */

int add(int x, int y)
{
return (x + y);
}
